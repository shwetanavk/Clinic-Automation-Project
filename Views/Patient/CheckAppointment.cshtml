
@{
    ViewBag.Title = "CheckAppointment";
}

<div class="jumbotron">

    <h2>
        @foreach (string error in ViewBag.res)
        {
            @Html.Label(error)
            <br />
        }
    </h2>
    <div class="row">
        <div class="col-md-4">
            <h3> </h3>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4">
            <label class="container"></label>
            <div class="col-md-2">
                <p><a href="@Url.Action("PatientLogin","Patient")" class="btn btn-primary btn-lg">Go Back</a></p>
            </div>
        </div>
    </div>
</div>